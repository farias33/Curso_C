#include <stdio.h>
#include <stdlib.h>
void main(){
    //Definindo variáveis
    int a = 1, b = 10;
    //Contando até 10
    while(a<=10){
        printf("\n%d", a);

        //Incremento
        a = a + 1; //a++ é a mesma coisa que a = a+1
        
    //Contagem regressiva
    while(b>=1){
        printf("\n%d", b);

        //Incremento
        b = b - 1; //b-- é a mesma coisa que b = b-1

    }






























    system("pause");
}